worker_processes 1;

events {
    worker_connections 4096;
    use epoll;
    multi_accept on;
}

stream {
    upstream api {
        least_conn;
        server api1:8080;
        server api2:8080;
    }

    server {
        listen 9999 reuseport;
        proxy_pass api;
    }
}
